# Docker Compose 用の例（必要なら .env にコピーして編集）
#
# 1) ローカルだけならこのままでOK（tokenは dev デフォルトの valid_token でも入れる）
# 2) スマホ/別端末から（Tailscale/LAN）使うなら、ALLOWED_ORIGINS に「実際にアクセスするURL」を入れてな
#
# 例:
# ALLOWED_ORIGINS=http://star.tail0d7567.ts.net:3101
# または
# ALLOWED_ORIGINS=http://100.120.81.67:3101

TERMINAL_TOKEN=valid_token
ALLOWED_ORIGINS=http://localhost:3101,http://127.0.0.1:3101
TERMINAL_LINK_TOKEN_TTL_SECONDS=300
TERMINAL_COOKIE_SECURE=0
BACKEND_NODE_ENV=development
